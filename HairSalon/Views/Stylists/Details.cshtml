@{
Layout = "_Layout";
}
@using HairSalon.Models
@model HairSalon.Models.Stylist

<h2>Stylist Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>
<h3>@Html.DisplayNameFor(model => model.Specialty): @Html.DisplayFor(model => model.Specialty) </h3>
<h4>Clients</h4>
@if(@Model.Clients.Count == 0)
{
  <h3>No Clients added yet!</h3>
  <p>@Html.ActionLink("Add new client","Create", "Clients")</p>
}
@foreach(Client client in Model.Clients)
{
  <li>@Html.ActionLink($"{client.Name}","Details", "Clients", new{id = client.ClientId})</li>
  
}
<p>@Html.ActionLink("Edit Stylist Information", "Edit", "Stylists", new{id = Model.StylistId})</p>
<p>@Html.ActionLink("Delete Stylist", "Delete", "Stylists")</p>
<p>@Html.ActionLink("Back to list", "Index")</p>